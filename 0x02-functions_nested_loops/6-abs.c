#include "holberton.h"


/**
 * int _abs(int) - prints absolute value of an integer
 * @i: integer to check the vale
 *
 * Return: a posive int or 0
 */
int _abs(int i)
{
	return (i > 0 ? i : -i);

}
