#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int n = 0;

	while (n < 10)
		putchar(n++ + '0');
	putchar(10);
	return (0);
}
