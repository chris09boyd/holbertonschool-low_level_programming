#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int n = 0;

	while (n < 10)
		printf("%d \n", n++);

	return (0);
}
